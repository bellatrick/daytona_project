version: 1
workspace:
  name: my-data-science-project
  containers:
    - image: python:3.9
      volumes:
        - .:/workspace
      env:
        - MY_ENV_VAR=some_value
  postCreateCommand: "pip install -r requirements.txt"
